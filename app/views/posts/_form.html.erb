<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post_text %>
    <%= form.text_field :post_text %>
  </div>

  <div class="field">
    <%= form.label :post_id %>
    <%#= form.number_field :post_id, id: :posts_post_id, value: current_user.id, type: :hidden %>
    <%= form.hidden_field :post_id, value: current_user.id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
